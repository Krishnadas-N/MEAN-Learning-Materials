import { ToggleDirectiveDirective } from './toggle-directive.directive';

describe('ToggleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
